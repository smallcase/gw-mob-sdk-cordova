repositories {
    maven {
        url "https://artifactory.smallcase.com/artifactory/gradle-dev-local"
        credentials {
            username "cordova_plugin_user"
            password "smallcaseCordovaPassword"
        }
    }
    mavenCentral()
}

dependencies {
implementation 'com.smallcase.gateway:sdk:3.4.1';
}